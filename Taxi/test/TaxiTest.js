const debug = require('debug')('test');
const _ = require('lodash');
const should = require('chai').should();
const sinon = require("sinon");
const Taxi = require('../src/Taxi');


describe('Taxi Test', () => {
  it('usage', () => {

  });

  it('success cases', () => {

  });
});